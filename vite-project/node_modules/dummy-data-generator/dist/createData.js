"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.randomNumber = exports.createParagraph = exports.createWord = void 0;
const consonants = "bcdfghjklmnpqrstvwxyzbpcrtpcrddrtplmnplmnbbcbcdrbnmklhgd";
const vowels = "aeiou";
function _jsUcfirst(string) {
    return string.charAt(0).toUpperCase() + string.slice(1);
}
const createWord = (length, capitalize = false) => {
    let word = "";
    let currently = "consonants";
    for (let i = 0; i < length; i++) {
        if (currently === "consonants") {
            word += consonants.substring(Math.floor(Math.random() * consonants.length), 1);
            currently = "vowels";
        }
        else {
            word += vowels.substring(Math.floor(Math.random() * vowels.length), 1);
            currently = "consonants";
        }
    }
    if (capitalize) {
        word = _jsUcfirst(word);
    }
    return word;
};
exports.createWord = createWord;
const createParagraph = (length) => {
    let paragraph = "";
    let capitalizeNextWord = true;
    for (let i = 0; i < length; i++) {
        paragraph += (0, exports.createWord)(2 + Math.floor(Math.random() * 9), capitalizeNextWord);
        if (Math.floor(Math.random() * 9) < 1) {
            paragraph += ". ";
            capitalizeNextWord = true;
        }
        else {
            paragraph += " ";
            capitalizeNextWord = false;
        }
    }
    return paragraph.trim() + ".";
};
exports.createParagraph = createParagraph;
const randomNumber = (length) => {
    let text = "";
    const possible = "123456789";
    for (let i = 0; i < length; i++) {
        const sup = Math.floor(Math.random() * possible.length);
        text += i > 0 && sup === i ? "0" : possible.charAt(sup);
    }
    return Number(text);
};
exports.randomNumber = randomNumber;
//# sourceMappingURL=createData.js.map